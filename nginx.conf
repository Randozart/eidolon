# Move the PID file to /tmp so it can be written in a read-only environment
pid /tmp/nginx.pid;

server {
    listen 8080;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Set temporary paths to /tmp (which will be our writable tmpfs)
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path       /tmp/proxy_temp_path;
    fastcgi_temp_path     /tmp/fastcgi_temp;
    uwsgi_temp_path       /tmp/uwsgi_temp;
    scgi_temp_path        /tmp/scgi_temp;

    # --- Security Headers ---
    server_tokens off;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' https://generativelanguage.googleapis.com; worker-src 'self' blob:;" always;

    # --- SPA Routing ---
    location / {
        try_files $uri $uri/ /index.html;
    }

    # --- WASM & MIME Support ---
    location ~* \.wasm$ {
        types { application/wasm wasm; }
        add_header Cache-Control "public, no-transform";
    }

    location ~* \.z[0-9]+$ {
        add_header Content-Type application/octet-stream;
    }
}